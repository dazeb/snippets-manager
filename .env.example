# Environment Variables Template
# Copy this file to .env.local and fill in your values

# Convex Development (automatically set by `convex dev`)
CONVEX_DEPLOYMENT=https://your-dev-deployment.convex.cloud

# For production builds (use this in hosting platforms like Vercel/Netlify)
# VITE_CONVEX_URL=https://your-production-deployment.convex.cloud

# For production deployment (DO NOT commit the actual key)
# CONVEX_DEPLOY_KEY=prod:your-deployment|your-key

# Environment
NODE_ENV=development
